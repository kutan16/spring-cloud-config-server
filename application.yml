eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:9011/eureka/,http://localhost:9012/eureka/,http://localhost:9013/eureka/
  instance:
    hostname: localhost
    
microservice:
  payment-service:
    endpoints:
      endpoint:
        uri: http://PAYMENT-SERVICE/payment-provider/paynow/
  post-service:
    endpoints:
      endpoint:
        uri: http://POST-SERVICE/post-service/postdata
  addcard-service:
    endpoints:
      endpoint:
        uri: http://localhost:8080/tokenizer/1/tokenmessage
